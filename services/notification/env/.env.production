NODE_ENV=production
VERSION=1.0.0
PORT=3005

RABBITMQ_URI=amqp://guest:guest@rabbitmq:5672
RMQ_NOTIFICATION_QUEUE=notification

SMTP_PREVIEW=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
# SMTP_USER, SMTP_PASS, and SMTP_FROM are loaded from env/.env.secrets
